{
  "$id": "blockception.minecraft.behavior.blocks.minecraft.connection_rule",
  "title": "Connection Rule",
  "description": "Allows custom blocks to define whether other blocks with connection behavior - such as fences, walls, bars, and glass panes - can try to create a connection.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "accepts_connections_from": {
      "title": "Accepts Connections From",
      "description": "The type of block allowed to connect to this block. Note that the \"only_fences\" option allows connections from all Vanilla fences excluding NetherBrick.",
      "type": "string",
      "enum": ["all", "none", "only_fences"],
      "default": "all"
    },
    "enabled_directions": {
      "title": "Enabled Directions",
      "description": "The cardinal directions that connection is enabled for. Note that if \"none\" is specified for \"accepts_connections_from\", this field will not be used.",
      "type": "array",
      "items": {
        "enum": ["east", "north", "south", "west"]
      },
      "default": ["east", "north", "south", "west"]
    }
  }
}

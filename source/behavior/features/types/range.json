{
  "$id": "blockception.minecraft.behavior.features.range",
  "title": "Range",
  "description": "A range of values. May be written as a single number, as a two-element array of the lower and upper bound, or as an object with `range_min` and `range_max`.",
  "examples": [4, [4, 6], { "range_min": 4, "range_max": 6 }],
  "oneOf": [
    { "type": "number", "title": "Value", "description": "A single value, used as both the lower and the upper bound." },
    {
      "type": "array",
      "title": "Range Array",
      "description": "The lower and upper bound of the range.",
      "minItems": 2,
      "maxItems": 2,
      "items": [
        { "type": "number", "title": "Min", "description": "The lower bound of the range." },
        { "type": "number", "title": "Max", "description": "The upper bound of the range." }
      ]
    },
    {
      "type": "object",
      "title": "Range",
      "description": "The lower and upper bound of the range.",
      "additionalProperties": false,
      "required": ["range_min", "range_max"],
      "properties": {
        "range_min": { "type": "number", "title": "Range Min", "description": "The lower bound of the range." },
        "range_max": { "type": "number", "title": "Range Max", "description": "The upper bound of the range." }
      }
    }
  ]
}

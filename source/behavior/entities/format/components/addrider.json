{
  "$id": "blockception.minecraft.behavior.entities.minecraft.addrider",
  "title": "Add Rider",
  "description": "Adds a rider to the entity. Requires `minecraft:rideable.`",
  "definitions": {
    "entry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["entity_type"],
      "properties": {
        "entity_type": {
          "type": "string",
          "description": "The entity type that will be riding this entity.",
          "title": "Entity Type"
        },
        "spawn_event": {
          "type": "string",
          "description": "The spawn event that will be used when the riding entity is created.",
          "title": "Spawn Event"
        }
      }
    }
  },
  "oneOf": [
    {
      "$ref": "#/definitions/entry"
    },
    {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "riders": {
          "title": "Riders",
          "description": "Array of riders to add to the entity",
          "type": "array",
          "items": {
            "$ref": "#/definitions/entry"
          }
        }
      }
    }
  ],
  "examples": [
    { "entity_type": "minecraft:rabbit" },
    { "entity_type": "minecraft:rabbit", "spawn_event": "self:example" }
  ]
}
